<!DOCTYPE html>
<html lang="pl">
<head>
    <meta charset="UTF-8">
    <title><%= Title || 'LiberalPost' %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>

<header class="site-header">
    <h1 class="logo"><a href="/">LiberalPost</a></h1>

    <nav class="nav">
        <a href="/">Artykuły</a>

        <% if (!user) { %>
            <a href="/auth/login">Login</a>
            <a href="/auth/register">Rejestracja</a>
        <% } else { %>
            <% if (user.role === 'admin') { %>
                <a href="/admin/posts">Panel admina</a>
            <% } %>
            <span class="user">Witaj, <%= user.username %></span>
            <a href="/auth/logout">Wyloguj</a>
        <% } %>
    </nav>
</header>
